version: '3.8'

services:
  taskmaster:
    build: .
    image: copilot-taskmaster:latest
    volumes:
      - ./tasks:/tasks
    environment:
      - TASKMASTER_TASKS_DIR=/tasks
    stdin_open: true
    tty: true
